cron="/usr/sbin/cron"

# cron will wait a random number of seconds between 0 and jitter to execute
# user cron jobs, and 0 and su_jitter for superuser cron jobs, this reduces
# spikes in load if too many cron jobs fall at the same time (ex. at the start
# of each hour)
jitter="10"
su_jitter="10"
